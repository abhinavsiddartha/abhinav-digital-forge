{
  "compilerOptions": {
    "target": "ES2020",
    "useDefineForClassFields": true,
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "skipLibCheck": true,

    /* Stable module resolution (important for deployment) */
    "moduleResolution": "node",

    /* Clean build setup */
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "react-jsx",

    /* Relaxed for faster dev (you can tighten later) */
    "strict": false,

    /* Path alias */
    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "include": ["src"]
}